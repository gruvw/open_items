import 'package:flutter/material.dart';

class ListsOrderingDialog extends StatelessWidget {
  final String accountId;

  const ListsOrderingDialog({
    super.key,
    required this.accountId,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
