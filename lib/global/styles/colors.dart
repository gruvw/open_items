import 'package:flutter/material.dart';

abstract class UIColors {
  static const Color primary = Colors.black;
  static const Color secondary = Colors.white;

  static const Color secondaryText = Color(0xFFA5A5A5);
  // static const Color danger = Colors.red;
  // static const Color textPrimary = Colors.black;
  // static const Color textPrimaryBold = Color.fromARGB(200, 0, 0, 0);
  // static const Color textContrasted = Colors.white;
  // static const Color divider = Colors.grey;
  // static const Color iconColor = Color.fromARGB(180, 0, 0, 0);
}
